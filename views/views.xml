<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="auth.list">
      <field name="name">auth list</field>
      <field name="model">auth.auth</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="auth.action_window">
      <field name="name">auth window</field>
      <field name="res_model">auth.auth</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="auth.action_server">
      <field name="name">auth server</field>
      <field name="model_id" ref="model_auth_auth"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="auth" id="auth.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="auth.menu_1" parent="auth.menu_root"/>
    <menuitem name="Menu 2" id="auth.menu_2" parent="auth.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="auth.menu_1_list" parent="auth.menu_1"
              action="auth.action_window"/>
    <menuitem name="Server to list" id="auth" parent="auth.menu_2"
              action="auth.action_server"/>
    -->
  </data>
</odoo>